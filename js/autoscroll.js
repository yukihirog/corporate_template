function autoScroll($target, $container){	if ($target) {		var pos = $target.offset();		if (pos) {			$container.stop().animate({ scrollTop : pos.top }, 800, 'swing');		}	}};$(document).on('click', function(e){	var $link = $(e.target).closest('[href^="#"]:not([aria-controles])');	if ($link.length) {		var hash = $link.attr('href');		if (hash !== '#') {			e.preventDefault();			var $target = $(hash);			if (!$target.length && hash === '#top') {				$target = $('body');			}			autoScroll($target, $('html, body'));		}	}});